# Generated from manifests/install/charts/compute-gardener-scheduler.
# DO NOT EDIT — rerun `make manifests` after changing the chart.
# Variant: standard
# Render command: helm template compute-gardener manifests/install/charts/compute-gardener-scheduler --namespace compute-gardener --set-string carbonAware.electricityMap.apiKey=YOUR_ELECTRICITY_MAP_API_KEY --set samplePod.enabled=false
---
apiVersion: v1
kind: Namespace
metadata:
  name: compute-gardener
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: compute-gardener-scheduler
  namespace: compute-gardener
---
apiVersion: v1
kind: Secret
metadata:
  name: compute-gardener-scheduler-secrets
  namespace: compute-gardener
type: Opaque
data:
  electricity-map-api-key: WU9VUl9FTEVDVFJJQ0lUWV9NQVBfQVBJX0tFWQ==
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: compute-gardener-scheduler-config
  namespace: compute-gardener
data:
  compute-gardener-scheduler-config.yaml: |
    apiVersion: kubescheduler.config.k8s.io/v1
    kind: KubeSchedulerConfiguration
    profiles:
      # SECONDARY MODE: Only handles pods that explicitly set schedulerName
      - schedulerName: compute-gardener-scheduler
        plugins:
          preFilter:
            enabled:
              - name: ComputeGardenerScheduler
          filter:
            enabled:
              - name: ComputeGardenerScheduler
    # Leader election configuration - determines whether this scheduler
    # participates in leader election with other scheduler instances
    leaderElection:
      leaderElect: false
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: compute-gardener-scheduler-metrics-reader
rules:
- apiGroups:
  - "metrics.k8s.io"
  resources:
  - nodes
  - pods
  verbs:
  - get
  - list
  - watch
- nonResourceURLs:
  - "/metrics"
  - "/api/v1/query"
  - "/api/v1/query_range"
  verbs:
  - get
---
# Add permissions for the scheduler to update pods for initial metrics annotations
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: compute-gardener-scheduler-pod-updater
rules:
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - list
  - watch
  - update
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: compute-gardener-scheduler-metrics-reader
subjects:
- kind: ServiceAccount
  name: compute-gardener-scheduler
  namespace: compute-gardener
roleRef:
  kind: ClusterRole
  name: compute-gardener-scheduler-metrics-reader
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: compute-gardener-scheduler-as-kube-scheduler
subjects:
- kind: ServiceAccount
  name: compute-gardener-scheduler
  namespace: compute-gardener
roleRef:
  kind: ClusterRole
  name: system:kube-scheduler
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: compute-gardener-scheduler-as-volume-scheduler
subjects:
- kind: ServiceAccount
  name: compute-gardener-scheduler
  namespace: compute-gardener
roleRef:
  kind: ClusterRole
  name: system:volume-scheduler
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: compute-gardener-scheduler-pod-updater
subjects:
- kind: ServiceAccount
  name: compute-gardener-scheduler
  namespace: compute-gardener
roleRef:
  kind: ClusterRole
  name: compute-gardener-scheduler-pod-updater
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: compute-gardener-scheduler-extension-apiserver-authentication-reader
  namespace: kube-system
roleRef:
  kind: Role
  name: extension-apiserver-authentication-reader
  apiGroup: rbac.authorization.k8s.io
subjects:
- kind: ServiceAccount
  name: compute-gardener-scheduler
  namespace: compute-gardener
---
apiVersion: v1
kind: Service
metadata:
  name: compute-gardener-dcgm-exporter
  namespace: compute-gardener
  labels:
    app: compute-gardener-dcgm-exporter
    component: gpu-metrics
    part-of: compute-gardener-scheduler
    helm.sh/chart: compute-gardener-scheduler-0.2.3
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/instance: compute-gardener
    app.kubernetes.io/version: v0.2.3-57ae4e1
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9400"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
  - port: 9400
    targetPort: 9400
    protocol: TCP
    name: metrics
  selector:
    app: compute-gardener-dcgm-exporter
---
apiVersion: v1
kind: Service
metadata:
  name: compute-gardener-scheduler-metrics
  namespace: compute-gardener
  labels:
    component: scheduler
    tier: control-plane
spec:
  ports:
  - name: https
    port: 10259
    targetPort: 10259
    protocol: TCP
  selector:
    component: scheduler
    tier: control-plane
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: compute-gardener-dcgm-exporter
  namespace: compute-gardener
  labels:
    app: compute-gardener-dcgm-exporter
    component: gpu-metrics
    part-of: compute-gardener-scheduler
    helm.sh/chart: compute-gardener-scheduler-0.2.3
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/instance: compute-gardener
    app.kubernetes.io/version: v0.2.3-57ae4e1
spec:
  selector:
    matchLabels:
      app: compute-gardener-dcgm-exporter
  template:
    metadata:
      labels:
        app: compute-gardener-dcgm-exporter
        component: gpu-metrics
        part-of: compute-gardener-scheduler
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9400"
        prometheus.io/path: "/metrics"
    spec:
      # Standard nodeSelector for NVIDIA GPU nodes
      nodeSelector:
        nvidia.com/gpu: present
      # Standard runtimeClassName for NVIDIA GPU access
      runtimeClassName: nvidia
      containers:
      - name: exporter
        image: nvcr.io/nvidia/k8s/dcgm-exporter:2.4.6-2.6.10-ubuntu20.04
        env:
        - name: NVIDIA_VISIBLE_DEVICES
          value: "all"
        - name: NVIDIA_DRIVER_CAPABILITIES
          value: "utility"
        securityContext:
          runAsNonRoot: false
          runAsUser: 0
          privileged: true
        ports:
        - containerPort: 9400
          name: metrics
          protocol: TCP
        volumeMounts:
        - name: nvidia-driver
          mountPath: /usr/local/nvidia
        - name: nvidia-driver-run
          mountPath: /run/nvidia/driver
        - name: dev
          mountPath: /dev
      volumes:
      - name: nvidia-driver
        hostPath:
          path: /usr/local/nvidia
      - name: nvidia-driver-run
        hostPath:
          path: /run/nvidia/driver
      - name: dev
        hostPath:
          path: /dev
      # These tolerations ensure the DaemonSet runs on GPU nodes, which might have taints
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    component: scheduler
    tier: control-plane
  name: compute-gardener-scheduler
  namespace: compute-gardener
spec:
  selector:
    matchLabels:
      component: scheduler
      tier: control-plane
  replicas: 1
  template:
    metadata:
      labels:
        component: scheduler
        tier: control-plane
        version: second
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '10259'
        prometheus.io/scheme: 'https'
        prometheus.io/path: '/metrics'
    spec:
      serviceAccountName: compute-gardener-scheduler
      containers:
      - command:
        - /bin/kube-scheduler
        - --config=/etc/kubernetes/compute-gardener-scheduler/compute-gardener-scheduler-config.yaml
        - --bind-address=0.0.0.0
        - --secure-port=10259
        - --authentication-skip-lookup=true
        - --authentication-tolerate-lookup-failure=true
        - --authorization-always-allow-paths=/metrics
        - --v=3
        image: docker.io/dmasselink/compute-gardener-scheduler:v0.2.3-57ae4e1
        imagePullPolicy: IfNotPresent
        env:
        - name: ELECTRICITY_MAP_API_KEY
          valueFrom:
            secretKeyRef:
              name: compute-gardener-scheduler-secrets
              key: electricity-map-api-key
        - name: CARBON_INTENSITY_THRESHOLD
          value: "200"
        - name: ELECTRICITY_MAP_API_REGION
          value: "US-CAL-CISO"
        - name: MAX_SCHEDULING_DELAY
          value: "24h"
        - name: HARDWARE_PROFILES_PATH
          value: "/etc/kubernetes/compute-gardener-scheduler/hardware-profiles/hardware-profiles.yaml"
        # Prometheus metrics configuration
        - name: PROMETHEUS_URL
          value: "http://prometheus-operated.monitoring.svc.cluster.local:9090"
        - name: PROMETHEUS_QUERY_TIMEOUT
          value: "30s"
        - name: PROMETHEUS_USE_DCGM
          value: "true"
        - name: PROMETHEUS_DCGM_POWER_METRIC
          value: "DCGM_FI_DEV_POWER_USAGE"
        ports:
        - containerPort: 10259
          name: https
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /healthz
            port: 10259
            scheme: HTTPS
          initialDelaySeconds: 15
        name: compute-gardener-scheduler-kube-scheduler
        readinessProbe:
          httpGet:
            path: /healthz
            port: 10259
            scheme: HTTPS
          initialDelaySeconds: 15
        resources: 
          limits:
            cpu: 200m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 128Mi
        securityContext:
          privileged: false
        volumeMounts:
          - name: config-volume
            mountPath: /etc/kubernetes/compute-gardener-scheduler
          - name: hardware-profiles-volume
            mountPath: /etc/kubernetes/compute-gardener-scheduler/hardware-profiles
      hostNetwork: false
      hostPID: false
      nodeSelector: 
        node-role.kubernetes.io/control-plane: "true"
      volumes:
        - name: config-volume
          configMap:
            name: compute-gardener-scheduler-config
        - name: hardware-profiles-volume
          configMap:
            name: compute-gardener-scheduler-hw-profiles
---
# Standard Prometheus Operator
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: compute-gardener-dcgm-exporter
  namespace: monitoring
  labels:
    app: compute-gardener-dcgm-exporter
    component: gpu-metrics
    part-of: compute-gardener-scheduler
    helm.sh/chart: compute-gardener-scheduler-0.2.3
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/instance: compute-gardener
    app.kubernetes.io/version: v0.2.3-57ae4e1
spec:
  selector:
    matchLabels:
      app: compute-gardener-dcgm-exporter
  endpoints:
  - port: metrics
    interval: 15s
    path: /metrics
  namespaceSelector:
    matchNames:
    - compute-gardener
---
# Standard Prometheus Operator.
# Prerequisite: the Prometheus Operator CRDs (specifically ServiceMonitor) must already
# be installed in the cluster, e.g.:
#   kubectl apply -f https://raw.githubusercontent.com/prometheus-operator/prometheus-operator/main/example/prometheus-operator-crd/monitoring.coreos.com_servicemonitors.yaml
# If you don't have Prometheus Operator, set metrics.serviceMonitor.enabled=false (or use
# the *-no-metrics raw manifest) to skip this resource.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: compute-gardener-scheduler-monitor
  # Default Prometheus namespace; change to match your setup (e.g. cattle-monitoring-system on Rancher).
  namespace: monitoring
spec:
  selector:
    matchLabels:
      component: scheduler
      tier: control-plane
  endpoints:
  - port: https 
    scheme: https
    path: /metrics
    interval: 15s
    bearerTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
    tlsConfig:
      insecureSkipVerify: true
  namespaceSelector:
    matchNames:
    - compute-gardener
